// When concatenating a string (value) with a number (4)
// the number will firstly be converted into a string literal by Javascript,
// before concatenation occurs.
const value = 3;
console.log(value + 4 + value);
